<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    >

    <data>
        <!-- 定义变量: var i -->
        <!-- Like : private org.kale.TestViewData vd-->
        <variable
            name="vd"
            type="org.kale.TestViewData"
            />

        <!-- Like: private org.kale.TestViewData vd02-->
        <variable
            name="vd02"
            type="org.kale.TestViewData02"
            />

    </data>

    <RelativeLayout
        android:id="@+id/root_rl"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        tools:context="kale.db.MainActivity"
        >

        <!-- Like: vd.getData()-->

        <TextView
            android:id="@+id/data_tv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="#ff0000"
            android:text="@{vd.data , default = data}"
            />

        <TextView
            android:id="@+id/package_name_tv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="50dp"
            android:text="@{vd.pkgName , default = pkgName}"
            />

        <Button
            android:id="@+id/packageName_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/package_name_tv"
            android:layout_centerHorizontal="true"
            android:text="@{vd02.btnText , default = Preview}"
            />

        <Button
            android:id="@+id/data_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:text="data"
            />

    </RelativeLayout>
</layout>